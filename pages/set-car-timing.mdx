---
name: Set Car Timing
menu: Pages
---

# Set Car Timing
The rental  car id is passing via URL `GET` parameter (`props.query.id`) and will we passed as `id` prop to [**SetCarTimingForm**](/SetCarTimingForm).
```jsx
 <SetCarTimingForm t={t} id={this.props.id} />
```